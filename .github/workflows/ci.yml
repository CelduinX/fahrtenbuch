name: Qualitätssicherung

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  secret-scan:
    name: Secret-Scan
    runs-on: ubuntu-latest
    steps:
      - name: Repository auschecken
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Geheimnisse erkennen
        uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7 # v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  application:
    name: Anwendung prüfen
    runs-on: ubuntu-latest
    env:
      DATABASE_PATH: ${{ runner.temp }}/fahrtenbuch-ci.db
      BACKUP_DIR: ${{ runner.temp }}/fahrtenbuch-backups
    steps:
      - name: Repository auschecken
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Node.js einrichten
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: npm
      - name: Abhängigkeiten installieren
        run: npm ci
      - name: Produktionsabhängigkeiten prüfen
        run: npm audit --omit=dev --audit-level=high
      - name: Code prüfen
        run: npm run lint
      - name: Typen prüfen
        run: npx tsc --noEmit
      - name: Tests ausführen
        run: npm test
      - name: Produktions-Build erstellen
        run: npm run build
      - name: Chromium installieren
        run: npx playwright install --with-deps chromium
      - name: Browsertests ausführen
        run: npm run test:e2e
