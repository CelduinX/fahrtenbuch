services:
  fahrtenbuch:
    image: ghcr.io/celduinx/fahrtenbuch:latest
    restart: unless-stopped
    init: true
    user: "0:0"
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      TZ: Europe/Berlin
    stop_grace_period: 20s
